# ===================================
#  Form Field Definitions
# ===================================

fields:
    name:
        label: Role Name
        type: text
        required: true
        span: left
    executive:
        label: Executive Position
        type: switch
        required:  true
        comment: Flick this switch to specify if this role is an executive role or not